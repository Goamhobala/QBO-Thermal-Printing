# QuickBooks OAuth Credentials
# Get these from https://developer.intuit.com
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
REDIRECT_URI=http://localhost:3000/redirect
FRONTEND_URL=http://localhost:5173

# QuickBooks Environment
# Options: 'sandbox' or 'production'
# - sandbox: Uses sandbox-quickbooks.api.intuit.com (for testing)
# - production: Uses quickbooks.api.intuit.com (for live data)
ENVIRONMENT=sandbox

# Session Secret (generate a random string)
SESSION_SECRET=your_random_session_secret_here

# Upstash Redis URL for session storage (optional for development)
# If not set, sessions will be stored in memory (lost on server restart)
# Get from: https://console.upstash.com (create free database)
REDIS_URL=redis://default:your_password@your-endpoint.upstash.io:6379

# Server
PORT=3000

# Node Environment (development or production)
NODE_ENV=development
