# QuickBooks OAuth Credentials - PRODUCTION
# Get these from https://developer.intuit.com (Production Keys tab)
CLIENT_ID=your_production_client_id
CLIENT_SECRET=your_production_client_secret
REDIRECT_URI=https://your-app-name.onrender.com/redirect
FRONTEND_URL=https://your-app-name.onrender.com

# Environment - MUST be 'production' for live QuickBooks
ENVIRONMENT=production

# Session Secret - Generate a strong random string
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
SESSION_SECRET=your_production_session_secret

# Upstash Redis Connection String (for session storage)
# Redis is ideal for sessions: fast, reliable, and has IPv4 support on free tier
# Get this from: https://console.upstash.com
# 1. Create account (free)
# 2. Create database (select region closest to your Render.com deployment)
# 3. Copy the Redis URL (starts with redis://)
# Free tier includes: 10,000 commands/day, 256MB storage (plenty for sessions)
REDIS_URL=redis://default:your_password@your-endpoint.upstash.io:6379

# Node Environment (Render sets PORT automatically, no need to specify)
NODE_ENV=production
